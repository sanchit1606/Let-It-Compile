# Install:
#   pip install -r requirements.txt
#
# Important (Windows + Numba):
# - Do NOT install `numba-cuda` in this environment.
# - Keep the pinned numba/llvmlite/numpy trio below together.

# PyTorch CUDA 12.1 wheels
--extra-index-url https://download.pytorch.org/whl/cu121
# PyG wheels matching torch 2.1.2 + cu121
--find-links https://data.pyg.org/whl/torch-2.1.2+cu121.html
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# Core numeric + CUDA JIT (known-compatible pins)
numpy==1.26.4
numba==0.59.1
llvmlite==0.42.0

# RL stack
stable-baselines3==2.3.2
gymnasium==0.29.1
scikit-learn==1.5.1

# Profiling / GPU telemetry
pynvml==11.5.3
cuda-python==12.6.2
# NVTX on Windows often requires local MSVC build tooling when installed via pip.
# Keep pip install reliable: skip nvtx on Windows, install via conda if needed:
#   conda install -n <env> -c conda-forge nvtx
nvtx==0.2.10; platform_system != "Windows"

# Data / plotting / utilities
pandas==2.2.2
matplotlib==3.8.4
seaborn==0.13.2
tqdm==4.66.5
rich==13.7.1
python-dotenv==1.0.1
requests==2.32.3

# Jupyter + developer tools
jupyter==1.1.1
ipykernel==6.29.5
pytest==8.3.2
black==24.8.0
isort==5.13.2
tensorboard==2.17.1

# GNN stack (resolved from PyG wheel index above)
torch-geometric==2.5.3
torch-scatter==2.1.2+pt21cu121
torch-sparse==0.6.18+pt21cu121
